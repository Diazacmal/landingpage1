const kontra = document.querySelector(".panahcinta");
window.addEventListener("scroll", function() {
    windowPosition = window.scrollY > 200;
    kotra.classList.toggle("panahcinta", windowPosition);
});